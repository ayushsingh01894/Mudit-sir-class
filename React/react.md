# React 
React is a JavaScript library for building user interfaces (UIs), especially for websites and web applications.

<React → UI banata hai
Vite → React project ko develop/run/build karne mein help karta hai>

Google v8 Engine js used in backend is used to execute 
in fronted browser is 

Asyncronous is used to 